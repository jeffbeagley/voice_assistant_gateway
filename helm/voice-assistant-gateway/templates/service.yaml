apiVersion: v1
kind: Service
metadata:
  name: {{ include "voice-assistant-gateway.fullname" . }}
  labels:
    {{- include "voice-assistant-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "voice-assistant-gateway.selectorLabels" . | nindent 4 }}
